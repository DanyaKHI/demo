# Для локальной разработки: НЕ ИЗМЕНЯЙ `.env` или `.env.development.template` (текущий файл),
# вместо этого создай .env.development.local (он уже под .gitignore),
# скопировав туда контент текущего файла.
